{
  "name": "meteora-weather",
  "version": "1.0.0",
  "description": "Lokale Wetter-App mit mehreren Designs, Radar/Karte, Offline-Cache und Wetter-Chat.",
  "scripts": {
    "build": "node scripts/build.js",
    "start": "node local-server.js",
    "check": "node --check local-server.js && node --check app.js && node --check api/chat.js && node --check scripts/build.js"
  },
  "keywords": [
    "weather",
    "wetter",
    "webview",
    "offline",
    "radar"
  ],
  "license": "MIT"
}
